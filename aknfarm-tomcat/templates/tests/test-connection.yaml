apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aknfarm-tomcat.fullname" . }}-test-connection"
  labels:
    {{- include "aknfarm-tomcat.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aknfarm-tomcat.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
